# Sacramento County sample rosters

Format-aligned with the Sheriff’s published field list and the Sacramento CSV /
PDF parser profiles. These are for Administrative Dashboard operational testing
until a real county export is available. Replace them via Import Inspection +
Parser Mapping when a live file arrives — do not treat these headers as final.
